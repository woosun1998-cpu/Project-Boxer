# Boxer — 여기부터 시작하세요

이 문서는 **프로젝트 루트(`Boxer` 폴더)** 기준으로, 처음부터 끝까지 따라 하기 좋은 순서입니다.  
모든 파일은 **UTF-8**로 저장하는 것을 권장합니다 (한글이 깨지지 않게).

---

## 1단계: 필수 소프트웨어

1. **Python 3.11 또는 3.12** (3.14 등 최신은 일부 패키지와 충돌할 수 있음)
2. **MySQL 8.x** (로컬에 설치되어 실행 중이어야 함)
3. **Git** (선택, 협업 시 필요)

---

## 2단계: 백엔드 환경 (`backend` 폴더)

1. `backend` 폴더에 `.env` 파일이 있는지 확인합니다.  
   - 없으면 `DB_PASSWORD`, `JWT_SECRET_KEY` 등을 채운 예시를 팀 규칙에 맞게 만듭니다.
2. 가상환경 생성 후 패키지 설치:

```bat
cd backend
py -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

3. DB 초기화:

```bat
python scripts\init_db.py
```

4. API 서버 실행:

```bat
py -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

5. 브라우저에서 확인: `http://localhost:8000/docs`

---

## 3단계: 프론트엔드 (`frontend` 폴더)

**중요:** 정적 서버의 **작업 폴더**에 따라 주소가 달라집니다.

- **권장:** `frontend` 폴더에서 서버 실행 → `http://localhost:5500/index.html`
- 예:

```bat
cd frontend
py -m http.server 5500
```

이 프로젝트는 링크를 **같은 폴더 기준 상대 경로**(`index.html`, `sparring.html`)로 맞춰 두었습니다.  
따라서 **`frontend`에서 서버를 띄우는 방식**이 가장 단순합니다.

---

## 4단계: 한 번에 실행 (Windows)

프로젝트 루트에 있는 배치 파일을 사용할 수 있습니다.

- `start-backend.bat` — 백엔드 (새 콘솔에서 실행 권장)
- `start-frontend.bat` — 프론트 (다른 콘솔에서 실행)

**팁:** 명령 프롬프트에서 한글이 깨지면 `chcp 65001` 후 다시 시도하세요.

---

## 5단계: 문제가 생기면

1. 백엔드: `http://localhost:8000/api/health` , `http://localhost:8000/api/health/db`
2. MySQL이 켜져 있는지, `.env`의 비밀번호·포트가 맞는지 확인
3. 프론트 404: 주소에 불필요한 `/frontend/` 가 붙었는지 확인 (권장 실행 방식은 위 3단계)

---

## 다음 읽을 문서 (정리 폴더)

- `정리/Boxer_실행_정리.md` — 실행 명령 모음
- `정리/Boxer_개선_우선순위_로드맵.md` — 개선 작업 순서·영상 정확도·UI·배포 방향

---

## 상용 배포를 목표로 할 때 (개요만)

- Docker + 리버스 프록시(Nginx) + HTTPS
- 환경 변수·비밀 관리 (서버에만 `.env`, Git에는 금지)
- DB 백업·마이그레이션(Alembic) 정책

**비용이 드는 클라우드·도메인·SSL 상품**을 쓰기 전에는 반드시 본인이 직접 선택해야 합니다.
