# Boxer 테스트 제외 실행 TODO

작성일: 2026-04-28
대상 폴더: `C:\Users\EZ\Boxer`
기준 브랜치: `boxer/donghyeon`

---

## 1) 작업 기준선 고정
- [ ] 현재 브랜치가 `boxer/donghyeon`인지 확인
- [ ] 표준 실행 포트 문서 고정 (백엔드 `8000`, 프론트 `5500`)
- [ ] 실행 명령 1세트만 유지하고 중복 문구 제거
- [ ] 오늘 유지 변경 / 보류 변경 구분 기록

완료 기준:
- [ ] 같은 명령으로 같은 화면을 재현 가능

---

## 2) 인코딩/문자 깨짐 정리
- [ ] 사용자 노출 한글(버튼/라벨/타이틀) 깨짐 우선 수정
- [ ] 소스/문서 UTF-8(무 BOM) 저장 상태 확인
- [ ] 줄바꿈 규칙(LF) 통일 점검 (`.editorconfig`, `.gitattributes`)

완료 기준:
- [ ] 주요 화면에서 한글 깨짐이 보이지 않음

---

## 3) 프론트 구조/리소스 정비
- [ ] 절대경로(`/frontend/...`) 사용 지점 정리 후 상대경로로 통일
- [ ] 이미지 참조 경로와 실제 파일(`frontend/assets/images`) 일치 점검
- [ ] 미사용/일회성 스크립트(`fix_*.py`) 분류 (보관/제거 후보 표시)

완료 기준:
- [ ] 화면 리소스 404 없음

---

## 4) API 계약 정리
- [ ] `frontend/js/core/api.js` 응답 처리 규격 통일 (`success`, `data`, `error`)
- [ ] 페이지별 API 실패 처리(토스트/경고문) 공통 패턴 적용
- [ ] 인증 흐름(`frontend/js/core/auth.js`)에서 토큰 처리 일관성 점검

완료 기준:
- [ ] API 실패 시 사용자 안내 메시지가 일관되게 노출됨

---

## 5) 스파링 품질 개선(기능)
- [ ] `attack_timestamps` 튜닝 대상 영상 우선순위 목록 작성
- [ ] 난이도별 기본값 분리 (`dodge_window_ms`, `hitbox_radius`)
- [ ] `PoseTracker.js` 핵심 파라미터(정확도/안정성) 표준값 문서화

완료 기준:
- [ ] 난이도별 판정 체감 차이가 명확해짐

---

## 6) UI 일관성 개선
- [ ] 공통 색상/간격/폰트는 `frontend/css/global.css` 토큰 기준으로 정리
- [ ] 로딩/오류 상태 메시지 스타일 통일
- [ ] 버튼 포커스/대비 등 접근성 기본 항목 반영

완료 기준:
- [ ] 페이지 간 UI 톤과 상호작용이 일관됨

---

## 7) 배포 준비
- [ ] `docker-compose` 기준 API+DB 로컬 표준 실행 흐름 정리
- [ ] Nginx 경로 규칙 점검 (`/` 정적, `/api` 백엔드)
- [ ] `.env.example` 최신화 (필수 키 누락 없음)

완료 기준:
- [ ] 새 환경에서 최소 실행 절차가 문서만으로 재현 가능

---

## 오늘 실행 우선순위 (빠르게)
1. 작업 기준선 고정
2. 인코딩/문자 깨짐 정리
3. 프론트 구조/리소스 정비

